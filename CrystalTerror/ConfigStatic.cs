namespace CrystalTerror;

/// <summary>
/// Static configuration values that don't change at runtime.
/// These are compile-time constants for UI layout and other fixed settings.
/// </summary>
public static class ConfigStatic
{
    /// <summary>
    /// Width of the retainer name column in the retainer table.
    /// </summary>
    public const float RetainerNameColumnWidth = 140.0f;

    /// <summary>
    /// Width of the job/level column in the retainer table.
    /// </summary>
    public const float JobLevelColumnWidth = 65.0f;
}
